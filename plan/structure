
main: Main(), version(), help()

actions/
    trigger: Trigger(), triggerParseInput(), triggerParseConfig()
    init: Init(), initParseInput(), initParseConfig()
    help: Help()

config/
    edit: Edit(global/local)
    help: Help()

template/
    template: Template(), struct{ dir, ... }, list(), add()
    manage: new(), remove(), update()
    help: Help()

license/
    license: License(), struct{ dir, ... }, list(), replace(), new()
    help: Help()

